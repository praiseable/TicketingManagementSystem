import { createColumnHelper, flexRender, getCoreRowModel, useReactTable } from '@tanstack/react-table';
import type { PerformanceSnapshot } from '@/types';
const helper = createColumnHelper<PerformanceSnapshot>();
const columns = [helper.accessor('periodKey', { header: 'Period' }), helper.accessor('issuesCompleted', { header: 'Completed' }), helper.accessor('timeLoggedSeconds', { header: 'Hours', cell: (info) => (info.getValue() / 3600).toFixed(1) }), helper.accessor('onTimePct', { header: 'On-time %', cell: (info) => `${Math.round(info.getValue())}%` })];
export function TeamTable({ data = [] }: { data?: PerformanceSnapshot[] }) { const table = useReactTable({ data, columns, getCoreRowModel: getCoreRowModel() }); return <div className="rounded-lg border"><table className="w-full text-sm"><thead>{table.getHeaderGroups().map((group) => <tr key={group.id}>{group.headers.map((h) => <th key={h.id} className="border-b p-3 text-left">{flexRender(h.column.columnDef.header, h.getContext())}</th>)}</tr>)}</thead><tbody>{table.getRowModel().rows.map((row) => <tr key={row.id}>{row.getVisibleCells().map((cell) => <td key={cell.id} className="border-b p-3">{flexRender(cell.column.columnDef.cell, cell.getContext())}</td>)}</tr>)}</tbody></table></div>; }
