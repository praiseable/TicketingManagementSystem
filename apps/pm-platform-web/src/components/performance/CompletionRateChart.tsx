import { RadialBar, RadialBarChart, ResponsiveContainer } from 'recharts';
export function CompletionRateChart({ assigned, completed }: { assigned: number; completed: number }) { const pct = assigned ? Math.round((completed / assigned) * 100) : 0; return <div className="relative h-72 rounded-lg border p-4"><ResponsiveContainer width="100%" height="100%"><RadialBarChart innerRadius="70%" outerRadius="95%" data={[{ name: 'Completion', value: pct }]} startAngle={90} endAngle={-270}><RadialBar dataKey="value" cornerRadius={8} /></RadialBarChart></ResponsiveContainer><div className="absolute inset-0 grid place-items-center text-3xl font-bold">{pct}%</div></div>; }
