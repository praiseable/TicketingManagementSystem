import { useQuery } from '@tanstack/react-query';
import { Link } from 'react-router-dom';
import { projectsApi } from '@/api/projects.api';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { EmptyState } from '@/components/common/EmptyState';
export function DashboardPage() { const { data: projects } = useQuery({ queryKey: ['projects'], queryFn: projectsApi.list }); return <div className="space-y-6"><h1 className="text-3xl font-bold">Dashboard</h1><div className="grid gap-4 md:grid-cols-3"><Card><CardHeader><CardTitle>Projects</CardTitle></CardHeader><CardContent><div className="text-3xl font-bold">{projects?.length ?? 0}</div></CardContent></Card><Card><CardHeader><CardTitle>Active work</CardTitle></CardHeader><CardContent><div className="text-3xl font-bold">Today</div></CardContent></Card><Card><CardHeader><CardTitle>Sprint status</CardTitle></CardHeader><CardContent><div className="text-3xl font-bold">Healthy</div></CardContent></Card></div><section><h2 className="mb-3 text-xl font-semibold">Recent projects</h2>{projects?.length ? <div className="grid gap-3 md:grid-cols-2">{projects.map((p) => <Link key={p.id} to={`/projects/${p.id}/board`} className="rounded-lg border p-4 hover:bg-accent"><div className="font-semibold">{p.name}</div><div className="text-sm text-muted-foreground">{p.key} · {p._count?.issues ?? 0} issues</div></Link>)}</div> : <EmptyState title="No projects" description="Create a project to start planning." />}</section></div>; }
